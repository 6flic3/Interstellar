name: Publish image to Docker Hub

on:
  [workflow_dispatch]

jobs:
  publish_image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: build
        run: |
          docker build . -t ${{ secrets.6flic3}}/${{ secrets.flic}}:latest
      - name: publish
        run: |
          docker login -u ${{ secrets.6flic3}} -p ${{ secrets.dckr_pat_arOKN9wxHyg4knwK0_W1cmsVSZI}}
          docker push ${{ secrets.6flic3}}/${{ secrets.flic}}:latest
